spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gutendex
    username: postgres
    password: senha
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080

logging:
  level:
    org.springframework.web: DEBUG
